# ============================================================
# Constitutional AI — Requirements
# Implementing Bai et al. 2022 with GRPO-KL on Qwen2-0.5B
# ============================================================

# Core training stack
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl>=0.9.0              # GRPOTrainer requires 0.9+. Tested with TRL 0.24.0.
transformers>=4.44.0
peft>=0.12.0
datasets>=2.20.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# API clients
groq>=0.9.0             # Groq Python SDK for llama-3.3-70b judge/critic

# Dashboard and visualization
streamlit>=1.35.0
plotly>=5.22.0
pyvis>=0.3.2
networkx>=3.3

# Logging and monitoring
tensorboard>=2.17.0
torch>=2.3.0

# Data processing
pandas>=2.0.0
pyarrow>=16.0.0         # For parquet file reading

# Utilities
sentencepiece
protobuf
pyngrok                  # For Colab dashboard tunneling via ngrok
requests

# =============================================================
# IMPORTANT — TRL 0.24.0 API renames (silent breaking changes):
#
#   GRPOConfig:
#     kl_coeff=       → beta=               (KL penalty coefficient)
#     max_new_tokens= → max_completion_length=
#
#   GRPOTrainer / SFTTrainer:
#     tokenizer=      → processing_class=
#
# Using old names is SILENTLY IGNORED — e.g., KL disabled with no error.
# This codebase uses the correct TRL 0.24.0 parameter names.
# =============================================================
