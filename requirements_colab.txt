# ============================================================
# Constitutional AI — Google Colab Requirements
# Conflict-free install order for Colab T4/A100
# ============================================================
#
# ROOT CAUSE of ImportError: cannot import '_unsloth_get_mm_token_id'
#   Colab pre-installs an old unsloth_zoo that doesn't match the
#   newer unsloth pulled from git. Fix: force-reinstall both together.
#
# INSTALL ORDER (run in a Colab cell):
#
#   # Step 0: Ensure pip is available and updated
#   !python -m ensurepip --upgrade
#   !python -m pip install --upgrade pip setuptools wheel
#
#   # Step 1: Install PyTorch 2.4.1 with CUDA 12.4
#   !pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu124
#
#   # Step 2: Install unsloth from GitHub (this pins compatible versions)
#   !pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#
#   # Step 3: Install training stack with exact versions
#   !pip install transformers==5.5.0 trl==0.24.0 peft==0.19.1 datasets==4.3.0 accelerate==1.13.0 bitsandbytes==0.49.2
#
#   # Step 4: Install remaining project dependencies
#   !pip install -r requirements_colab.txt
#
#   # Step 5: Restart runtime (Runtime → Restart runtime)
#
# CRITICAL NOTES:
#   - transformers MUST be 5.5.0 (not 4.x) for trl 0.24.0 compatibility
#   - torch MUST be 2.4.1 (not 2.5+) for Windows torchao wheel availability
#   - Install unsloth BEFORE other training libs so it can manage versions
# ============================================================

# ── Essential build tools ────────────────────────────────────────────────
pip>=24.0
setuptools>=70.0.0
wheel>=0.43.0

# Core training stack — EXACT versions to match local requirements
# DO NOT use >= ranges here; they cause version conflicts
transformers==5.5.0
trl==0.24.0
peft==0.19.1
datasets==4.3.0
accelerate==1.13.0
bitsandbytes==0.49.2

# torchao — CRITICAL: must be compatible with torch 2.4.1
# torchao 0.17.0+ requires torch.int1 (added in torch 2.5+)
torchao==0.13.0

# Note: unsloth should be installed separately via git (see instructions above)
# Do NOT list it here to avoid version conflicts

# Groq API judge
groq>=0.9.0

# Dashboard & visualization
streamlit>=1.35.0
plotly>=5.22.0
pyvis>=0.3.2
networkx>=3.3

# Logging
tensorboard>=2.17.0

# Data processing
pandas>=2.0.0
pyarrow>=16.0.0
sentencepiece
protobuf

# Colab tunnel
pyngrok
